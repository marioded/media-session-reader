# Media session reader

A cross-platform rust library to read the currently playing media session (title, artist, duration and playback position)
The goal is to provide a unified API over different operating systems

---

## Installation

Add the dependency to your `Cargo.toml`

```toml
[dependencies]
media-session-reader = { path = "../media-session-reader" }